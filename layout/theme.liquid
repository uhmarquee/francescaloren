<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page.title | default: 'Francesca Loren' }}</title>
  {{ content_for_header }}
  
  <style>
    * {
      box-sizing: border-box;
    }
    body, html {
      margin: 0;
      padding: 0;
      overflow-x: hidden;
    }

    /* ==================== NAVBAR ==================== */
    .navbar {
      background-color: #fff;
      border: 2px solid #000;
      padding: 18px 15px;           /* Increased to create visible white space on sides */
      position: relative;
      z-index: 1001;
      font-family: Arial, sans-serif;
      margin-bottom: 15px;          /* Adds spacing between navbar and image */
    }

    .navbar-inner {
      display: flex;
      align-items: center;
      justify-content: center;
      max-width: 1200px;
      margin: 0 auto;
    }

    .logo-link {
      color: #000;
      font-size: 42px;
      font-weight: bold;
      text-decoration: none;
      font-style: italic;
      letter-spacing: 3px;
      white-space: nowrap;
    }

    /* ==================== IMAGES ==================== */
    .image-container-1 {
      width: 100%;
      position: relative;
      padding: 0 15px;              /* Same side padding as navbar for perfect alignment */
      margin-top: 0;
    }

    .image-1 {
      width: 100%;
      height: auto;
      object-fit: cover;
      display: block;
      border: 2px solid #000;
    }

    /* ==================== RESPONSIVE ==================== */
    @media screen and (max-width: 768px) {
      .navbar {
        padding: 14px 12px;         /* Slightly more side space on mobile */
        margin-bottom: 12px;
      }
      .logo-link {
        font-size: 22px;
        letter-spacing: 1.5px;
      }
      .image-container-1 {
        padding: 0 12px;
      }
      .image-1 {
        border-width: 1.5px;
      }
    }

    /* Footer */
    footer {
      width: 100%;
      position: static;
    }
    .copyright_text {
      font-size: 18px;
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 25px 0;
    }
  </style>
</head>
<body>

  <!-- Navbar with matching side white space -->
  <nav class="navbar">
    <div class="navbar-inner">
      <a href="/" class="logo-link">FRANCESCA LOREN</a>
    </div>
  </nav>

  <main>
    {{ content_for_layout }}
  </main>

  {% section 'footer' %}
  {{ content_for_footer }}
</body>
</html>
