<style>
  /* Base styles (works on all devices) */
  .navbar {
    margin-top: 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #fff;
    border: 2px solid #000;
    padding: 20px 15px;           /* smaller padding on mobile */
    color: #000;
    font-family: Arial, sans-serif;
    position: relative;
    z-index: 1001;
  }

  .logo-container {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
  }

  .logo-link {
    color: #000;
    font-size: 28px;              /* slightly smaller on mobile */
    font-weight: bold;
    text-decoration: none;
    font-style: italic;
  }

  .image-container-1 {
    margin-top: 10px;
    width: 100%;
    position: relative;
    overflow: hidden;             /* prevents weird scrolling */
  }

  .image-1 {
    width: 100%;
    height: auto;                 /* important for mobile */
    object-fit: cover;
    display: block;
    border: 2px solid black;
  }

  footer {
    width: 100%;
  }

  .copyright_text {
    font-size: 18px;
    font-family: Arial, sans-serif;
    text-align: center;
    padding: 30px 0;
  }

  /* ===================== MOBILE FIXES ===================== */
  @media (max-width: 768px) {
    .navbar {
      padding: 15px 10px;
    }

    .logo-link {
      font-size: 24px;            /* smaller logo on phones */
    }

    .image-container-1 {
      height: auto !important;    /* removes your huge 4400px */
      min-height: 500px;          /* you can change this number */
    }

    .image-1 {
      height: 100vh;              /* full screen height on mobile - dramatic look */
      max-height: 700px;          /* safety limit so it doesn't get too tall */
    }
  }

  /* Optional: Extra fix for very small phones */
  @media (max-width: 480px) {
    .logo-link {
      font-size: 22px;
    }
  }
</style>
