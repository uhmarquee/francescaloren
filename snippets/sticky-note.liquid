<!-- =============== STICKY NOTE =============== -->
<div class="sticky-note windows-image-container" id="stickyWindow">
    <div class="title-bar" id="titleBar">
        <span class="title">Sticky Note</span>
        <div class="buttons">
            <button class="btn minimize-btn" id="minBtn"></button>
            <button class="btn maximize-btn" id="maxBtn"></button>
            <button class="btn close-btn" id="closeBtn"></button>
        </div>
    </div>
    <div class="stickynote-content windows-image-content" id="contentArea">
        <textarea id="noteText">Hi, my nickname is Harle nice to meet ya! Sorry, I'm busy right now, but I'll see ya soon!
-HQ</textarea>
    </div>
</div>

<style>
/* ==================== STICKY NOTE STYLES ==================== */
.sticky-note {
    position: absolute;
    top: 1200px;
    margin-top: 900px;
    left: 50%;
    transform: translateX(-211%);
    width: 700px;
    max-width: 92vw;
    height: 600px;
    background: #fff59d;
    border: 2px solid white;
    box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
    z-index: 9999;
    overflow: hidden;
    font-family: Arial, sans-serif;
    transition: width 0.25s ease, height 0.25s ease, transform 0.25s ease;
}

/* Title bar, buttons, content styles (unchanged) */
.title-bar { /* ... same as before ... */ }
.title { font-weight: bold; font-size: 12px; }
.buttons { display: flex; gap: 2px; }
.btn { /* ... same ... */ }
/* minimize-btn, maximize-btn, close-btn pseudo elements same as before */

.stickynote-content {
    padding: 10px;
    height: calc(100% - 25px);
    background: #fff59d;
    display: flex;
    flex-direction: column;
}

.stickynote-content textarea {
    width: 100%;
    height: 100%;
    background: transparent;
    border: none;
    outline: none;
    font-size: 15.5px;
    line-height: 1.5;
    resize: none;
    font-family: Arial, sans-serif;
    color: black;
}

/* ==================== STATES ==================== */
.minimized .stickynote-content { display: none; }
.minimized { height: 25px !important; overflow: hidden; }

.enlarged {
    width: 820px !important;
    height: 720px !important;
    z-index: 10000;
}

/* ==================== RESPONSIVE RULES ==================== */
/* TABLETS & Small Laptops */
@media screen and (max-width: 1024px) {
    .sticky-note { 
        width: 460px; 
        height: 380px; 
    }
}

/* MOBILE PHONES + Smaller Desktops */
@media screen and (max-width: 1500px) {
    .sticky-note {
        top: 900px !important;
        margin-top: 650px !important;
        left: 50% !important;
        transform: translateX(-80%) !important;   /* Your updated normal mobile position */
        width: 60vw;
        height: 280px;
    }

    /* Enlarged on mobile - expands to the right while trying to keep left alignment */
    .sticky-note.enlarged {
        width: 85vw !important;           /* Wider on mobile */
        height: 420px !important;         /* Adjust height as needed */
        transform: translateX(-580%) !important;   /* Your requested value */
        left: 50% !important;
    }
}

/* VERY SMALL PHONES */
@media screen and (max-width: 480px) {
    .sticky-note { 
        top: 70px !important; 
        height: 280px; 
    }
    .stickynote-content textarea { 
        font-size: 14.5px; 
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const stickyWindow = document.getElementById('stickyWindow');
    const titleBar = document.getElementById('titleBar');
    const minBtn = document.getElementById('minBtn');
    const maxBtn = document.getElementById('maxBtn');
    const closeBtn = document.getElementById('closeBtn');

    let isMinimized = false;
    let isEnlarged = false;
    let originalStyles = {};

    function saveOriginalStyles() {
        originalStyles = {
            width: stickyWindow.style.width || getComputedStyle(stickyWindow).width,
            height: stickyWindow.style.height || getComputedStyle(stickyWindow).height,
            transform: stickyWindow.style.transform
        };
    }

    // Dragging (unchanged)
    let isDragging = false;
    let offsetX, offsetY;

    function startDrag(e) {
        if (e.target.tagName === 'BUTTON') return;
        isDragging = true;
        const rect = stickyWindow.getBoundingClientRect();
        const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
        const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;
        offsetX = clientX - rect.left;
        offsetY = clientY - rect.top;
        stickyWindow.style.transform = 'none';
    }

    function drag(e) {
        if (!isDragging) return;
        const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
        const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;
        stickyWindow.style.left = (clientX - offsetX) + 'px';
        stickyWindow.style.top = (clientY - offsetY) + 'px';
    }

    titleBar.addEventListener('mousedown', startDrag);
    document.addEventListener('mousemove', drag);
    document.addEventListener('mouseup', () => isDragging = false);

    titleBar.addEventListener('touchstart', startDrag, { passive: true });
    document.addEventListener('touchmove', drag, { passive: true });
    document.addEventListener('touchend', () => isDragging = false);

    // Minimize
    minBtn.addEventListener('click', () => {
        if (isMinimized) {
            stickyWindow.classList.remove('minimized');
            isMinimized = false;
        } else {
            stickyWindow.classList.add('minimized');
            isMinimized = true;
        }
    });

    // Maximize / Enlarge
    maxBtn.addEventListener('click', () => {
        if (isEnlarged) {
            stickyWindow.style.width = originalStyles.width;
            stickyWindow.style.height = originalStyles.height;
            stickyWindow.style.transform = originalStyles.transform;
            stickyWindow.classList.remove('enlarged');
            isEnlarged = false;
        } else {
            saveOriginalStyles();
            stickyWindow.classList.add('enlarged');
            isEnlarged = true;
        }
    });

    // Close
    closeBtn.addEventListener('click', () => {
        stickyWindow.style.display = 'none';
    });

    saveOriginalStyles();
});
</script>
