<!-- =============== STICKY NOTE =============== -->

<div class="sticky-note" id="stickyWindow">
  <div class="title-bar" id="titleBar">
    <span class="title">Sticky Note</span>
    <div class="buttons">
      <button class="btn" id="minBtn">_</button>
      <button class="btn" id="maxBtn">□</button>
      <button class="btn close-btn" id="closeBtn">✕</button>
    </div>
  </div>
  <div class="stickynote-content" id="contentArea">
    <textarea id="noteText">Hi, my nickname is Harle nice to meet ya! Sorry, I'm busy right now, but I'll see ya soon!
-HQ</textarea>
  </div>
</div>

<style>
  .sticky-note {
    margin-top: 500px;
  position: fixed !important;           /* Keeps it in one place while scrolling */
    top: 650px;                /* Move this number higher/lower as needed */
    left: 50%;
    transform: translateX(-50%);
    width: 500px;
    height: 400px;
    background: #fff59d;
    border: 3px solid #e0c040;
    box-shadow: 8px 8px 25px rgba(0, 0, 0, 0.5);
    z-index: 9999;
    overflow: hidden;
  }

  .title-bar {
    background: linear-gradient(to right, #000080, #1084d0);
    color: white;
    padding: 6px 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: move;
    user-select: none;
    height: 32px;
  }

  .title {
    font-weight: bold;
    font-size: 14px;
  }

  .buttons {
    display: flex;
    gap: 4px;
  }

  .btn {
    width: 24px;
    height: 22px;
    background: #c0c0c0;
    border: 1px solid #dfdfdf;
    border-right-color: #808080;
    border-bottom-color: #808080;
    font-size: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }

  .close-btn:hover {
    background: #e51400;
    color: white;
  }

  .stickynote-content {
    height: calc(100% - 32px);
    padding: 15px;
    background: #fff59d;
  }

  .stickynote-content textarea {
    width: 100%;
    height: 100%;
    background: transparent;
    border: none;
    outline: none;
    font-size: 16px;
    line-height: 1.5;
    resize: none;
    font-family: Arial, sans-serif;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const stickyWindow = document.getElementById('stickyWindow');
    const titleBar = document.getElementById('titleBar');
    const minBtn = document.getElementById('minBtn');
    const maxBtn = document.getElementById('maxBtn');
    const closeBtn = document.getElementById('closeBtn');
    const contentArea = document.getElementById('contentArea');

    let isMaximized = false;

    let isDragging = false;
    let offsetX, offsetY;

    titleBar.addEventListener('mousedown', (e) => {
      if (e.target.tagName === 'BUTTON') return;
      isDragging = true;
      offsetX = e.clientX - stickyWindow.offsetLeft;
      offsetY = e.clientY - stickyWindow.offsetTop;
    });

    document.addEventListener('mousemove', (e) => {
      if (isDragging) {
        stickyWindow.style.left = (e.clientX - offsetX) + 'px';
        stickyWindow.style.top = (e.clientY - offsetY) + 'px';
        stickyWindow.style.transform = 'none';
      }
    });

    document.addEventListener('mouseup', () => isDragging = false);

    minBtn.addEventListener('click', () => {
      contentArea.style.display = 'none';
      stickyWindow.style.height = '32px';
    });

    maxBtn.addEventListener('click', () => {
      if (!isMaximized) {
        stickyWindow.style.width = '90vw';
        stickyWindow.style.height = '85vh';
        stickyWindow.style.top = '5vh';
        stickyWindow.style.left = '5vw';
        stickyWindow.style.transform = 'none';
        maxBtn.textContent = '❐';
        isMaximized = true;
      } else {
        stickyWindow.style.width = '500px';
        stickyWindow.style.height = '400px';
        stickyWindow.style.top = '650px';
        stickyWindow.style.left = '50%';
        stickyWindow.style.transform = 'translateX(-50%)';
        maxBtn.textContent = '□';
        isMaximized = false;
      }
      contentArea.style.display = 'block';
    });

    closeBtn.addEventListener('click', () => {
      stickyWindow.style.display = 'none';
    });
  });
</script>
